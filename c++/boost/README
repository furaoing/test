Prerequisites:
    boost headers placed in /usr/includes
    boost .so library placed in /usr/lib (optional)


Compilation Command:
g++ main.cpp -o main.exe -lboost_filesystem -lboost_system -pthread -std=c++11